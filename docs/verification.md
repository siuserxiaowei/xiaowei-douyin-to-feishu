# 验证记录

日期：2026-09-25。对象：`xiaowei-douyin-to-feishu` v1.2.0。

## 已有真实运行

在豆包工作的一条公开视频上，浏览器会话 + `yt-dlp` 取得音频，mediakit Cloud ASR 返回 142 段、1241 字口播；飞书创建后读回全文，与原始逐字稿一致。ASR 未人工逐字校对。这个运行证明该环境中音频路径可用，不证明所有视频都可访问。

v1.1.0 重新在线运行下载脚本，取得页面 `description`、224 秒视频时长元数据和 7,177,178 字节 WAV 音轨；再用此前保存的真实 mediakit 分段 JSON 组装。结果为 `complete`、142 段、1241 字，生成的文字与原 `transcript.txt` 逐字一致，字符数和 SHA-256 一致；运行记录分别保留发布文案及音频/ASR 路径。新版飞书脚本还只读复核了此前已有文档，仍返回 `verified_full_text`，未重复创建。此集成检查没有重新调用云端 ASR，也没有再建飞书文档。临时音频与测试输出已清理。

## v1.2.0 新增流程与验证边界

新增 `references/terminology-review.md`，要求在交付前扫描行业概念、专名、缩写和数字单位，保留 ASR 原稿，只依据音频或可信资料做局部修订，并记录未确认词和证据。它是 Skill 的复核工作流，不是新的 ASR 引擎或专用词库。

这套流程尚未在术语密集视频上做真实对照验证，也未验证术语候选召回率或纠错准确率；现有一条 ASR 稿件未人工术语校对。发布时必须保持这个限制说明，不能把“新增复核步骤”宣传成“已验证专业术语都能准确识别”。

## 既有版本检查

| 检查 | 结果 |
|---|---|
| Python 离线回归 | 46 项通过，覆盖下载元数据、独立音频时长回退、ASR 完成/缺段/失败、正文保真与飞书续跑 |
| 实际 ASR 分段重组 | 已通过：真实 142 段重组为 1241 字，逐字匹配上次原稿 |
| Skill 格式、脚本语法与差异检查 | quick_validate、四个脚本 py_compile、git diff --check 均通过 |
| 安装包 | 10 个文件；ZIP CRC、与源文件逐字节一致、已安装豆包工作版本与 ZIP 逐字节一致 |

在仓库根目录运行：

~~~bash
python3 -m unittest discover -s tests -v
~~~

## 边界

上游参考仓库依赖豆包工作内部的 `web.fetch` 获取口播，并按 `end_offset` / `total_length` 分页；上次的豆包工作环境未暴露此工具，因此**内置快路径尚未在该环境实测**。v1.1.0 只会在当前会话确有该能力时调用，不声称安装本 Skill 就会拥有它。

本版新增“仅测试提取”模式，尚待用户在豆包工作以新公开视频执行。批量主页采集、追加到已有飞书文档、飞书妙记和本地 SenseVoice 也未实测。自动转写与原稿完全一致只证明组装未丢字，不代表识别没有同音字或断句错误。v1.0.0 和 v1.0.1 的发行包保持不变。
