# 验证记录

日期：2026-09-24。对象：`xiaowei-douyin-to-feishu` v1.0.0。

## 已执行

| 检查 | 结果与边界 |
|---|---|
| Python 离线回归测试 | 21 项通过，覆盖分享文本、ID 提取、最终跳转、页面 JSON、异常页面、受限下载、错误记录、正文保留、去重拒绝和不覆盖已有文件 |
| FFmpeg 实际执行 | 将本地合成的双声道 44.1 kHz WAV 转为单声道 16 kHz WAV，并读取音频参数核对；未使用真实用户媒体 |
| Skill quick_validate | 通过；Python 主环境没有 PyYAML，使用临时 uv 环境运行验证器，未更改系统 Python 依赖 |
| 相对引用及 Python 语法 | 通过；包内引用文件存在，两个脚本语法可解析 |
| 实际 CommonMark 渲染 | 用 markdown-it-py 渲染样稿后提取正文，与原稿逐字一致，覆盖重复句、HTML 实体和特殊符号；不代表飞书服务端渲染已验证 |
| 飞书 CLI 命令 | 对本机当前 `+create` / `+update` / `+fetch` help 核对参数 |
| 飞书创建 dry-run | `ok: true`、`dry_run: true`，生成 Markdown 创建请求；没有远端创建文档 |

可复跑命令（在仓库根目录）：

```bash
python3 -m unittest discover -s tests -v
```

测试文件 `tests/test_douyin_skill.py` 使用人工文本和临时目录，覆盖重复句、普通词“获取”、不足 50 字的情况以及 Markdown/XML 特殊符号。测试视频 ID 是人工构造，不是已采集的视频。开发阶段的带本地路径运行产物未纳入仓库。

## 按任务情境审阅

- 没有飞书地址：默认新建文档，不询问表格地址。
- 已有文档：先读取、按 ID 去重、追加，禁止把整篇清空重写。
- 主页作品置顶：按可证实的发布时间排序，不把首屏顺序直接称为最新。
- 短口播：短于 50 字和末尾无标点本身不触发失败。
- 分页不完整：保留部分结果，明确标识缺段并走可用回退。
- 写入超时：先读回定位，不直接再次创建或重复追加。
- 无法确认无口播：空结果标作未取得，不能自行推断视频无口播。
- 音频回退缺模型：报告缺失依赖，默认不安装或下载大模型。

以上是指令审阅，不等同于在真实豆包工作里的行为测试。

## 未验证

豆包工作的真实 Skill 安装、内置 `web.fetch` 取稿、真实抖音分享页下载、真实 SenseVoice 识别质量、飞书妙记任务和飞书远端创建/追加/读回均未验证。

没有真实链接、目标豆包工作运行环境和该会话内的飞书连接器执行结果，不能把本地测试结论扩大为在线端到端成功。首轮使用应以一条真实公开视频完成闭环。

## 交付包

`xiaowei-douyin-to-feishu-v1.0.0.zip`，共 8 个文件。ZIP CRC、解压后文件与源文件逐字节比对、包内相对引用检查均通过。未包含测试素材、缓存、凭据或模型权重。

SHA-256：`9ec408beb4f9be3b3f7a715ed020cd26d7c857f973d5e41b8fb1a48617102feb`。
